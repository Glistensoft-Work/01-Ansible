{
  "sensu": [
    {
      "name": "{{ansible_hostname}}",
      "host": "localhost",
      "port": 4567,
      "timeout": 10
    }
  ],
  "uchiwa": {
    "host": "0.0.0.0",
    "port": 3000,
    "refresh": 10,
    "stats": 10,
    "user": "{{uchiwa_username}}",
    "pass": "{{uchiwa_password}}"
  }
}
